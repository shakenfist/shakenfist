{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "gitAuthor": "shakenfist-bot <bot@shakenfist.com>",
  "dependencyDashboard": true,
  "assignees": [
    "mikalstill"
  ],
  "schedule": [
    "after 9pm"
  ],
  "pre-commit": {
    "description": "Renovate's pre-commit manager is opt-in. Without this the hook revisions in .pre-commit-config.yaml are the one dependency file nobody is watching.",
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Automatically merge minor and patch-level updates",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest"
      ],
      "automerge": false
    },
    {
      "description": "Group pydantic updates together",
      "groupName": "pydantic",
      "matchPackageNames": [
        "/pydantic/"
      ]
    },
    {
      "description": "Group grpcio updates together. grpcio and protobuf are coupled (each grpcio release pins a protobuf range), so major and minor updates must land in a single PR or neither can pass dependency resolution",
      "groupName": "grpcio",
      "separateMajorMinor": false,
      "matchPackageNames": [
        "/grpcio/",
        "/protobuf/"
      ]
    },
    {
      "description": "Group oslo updates together. The OpenStack oslo libraries are released in lockstep and share internal interfaces, so they must update together. The pattern matches the family rather than listing today's members, so a fifth oslo library arriving as a transitive pin is grouped without anyone having to notice",
      "groupName": "oslo",
      "matchPackageNames": [
        "/^oslo\\./"
      ]
    },
    {
      "description": "Group zope updates together",
      "groupName": "zope",
      "matchPackageNames": [
        "zope.event",
        "zope.interface"
      ]
    }
  ],
  "minimumReleaseAge": "3 days",
  "rollbackPrs": true
}
