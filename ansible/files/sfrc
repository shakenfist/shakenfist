# Command line hinting
eval "$(_SF_CLIENT_COMPLETE=source sf-client)"

# Use the v3 etcd API
export ETCDCTL_API=3

# Client auth
SHAKENFIST_USERNAME="admin"
SHAKENFIST_PASSWORD="{{api_admin_password}}"
SHAKENFIST_API_URL="http://localhost:13000"