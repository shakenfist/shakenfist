---
- debug:
    msg: "Executing hypervisor role action {{ role_action }}."
  delegate_to: localhost
  run_once: true

- include_tasks: "{{ role_action }}.yml"

- debug:
    msg: "Completed hypervisor role action {{ role_action }}."
  delegate_to: localhost
  run_once: true